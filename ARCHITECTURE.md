# drama-copyright-check Skill 架构文档

## 文件结构

```
drama-copyright-check.skill   ← ZIP 压缩包
└── drama-copyright-check/
    ├── SKILL.md              ← Skill 核心，全部 prompt 逻辑在此
    ├── prepare.py            ← 可选预处理脚本（格式转换 + 分段）
    └── export.py             ← 可选导出脚本（Markdown 输出 → .docx）
```

Skill 本质是一个重命名为 `.skill` 的 ZIP 文件。SKILL.md 包含两部分：
- **frontmatter**（YAML，`---` 包裹）：元数据，供宿主系统识别与路由
- **正文**（Markdown）：给 Claude 的自然语言操作指令

`prepare.py` 在调用 skill 前独立运行，负责将 `.doc / .docx / .pdf / .txt` 统一转为纯文本，并在超过 8 万字时自动切分为多段文件。

`export.py` 在 skill 输出后按需运行，将 Markdown 格式的对照表导出为带样式的 `.docx` 文件；支持从剪贴板或 `.txt` 文件读取输入。

---

## Frontmatter 字段

| 字段 | 值 | 作用 |
|---|---|---|
| `name` | `drama-copyright-check` | Skill 唯一标识，用于调用与日志 |
| `description` | 长文本 | 触发判断依据，Claude 根据此描述决定是否激活 skill |

触发关键词（来自 description）：剧本侵权、版权分析、剧本对比、侵权风险、著作权、短剧合规、抄袭对比。

---

## 处理流程架构

```
原始文件（.doc / .docx / .pdf / .txt）
   │
   ▼
[前置] prepare.py（skill 外独立运行）
   ├── 格式转换 → 统一为纯文本
   └── 长度检测
       ├── ≤ 8万字 ──→ 输出 full_text.txt，直接传入 skill
       └── > 8万字 ──→ 按分集标记切分（无标记则5万字均分）
                           └── 输出 part_1.txt、part_2.txt…，逐段传入 skill
   │
   ▼  （以下为 skill / Claude 执行范围）
   │
   ▼
[第一步] 输入识别
   ├── 1个剧本 ──→ 询问是否需要空白对方列
   └── 2个剧本 ──→ 确认我方 / 对方归属
   │
   ▼
[第二步] 结构提取（每个剧本独立执行）
   ├── 人物：身份 + 性格 + 叙事功能 + 关系网络
   └── 情节节点：场景级粒度，含名称 + 核心事件 + 因果关系
   │
   ▼
[第三步] 人物对照表
   ├── 单剧本：2列（角色 | 我方人设）
   └── 双剧本：3列（角色 | 我方人设 | 对方人设），末行写关系网络
   │
   ▼
[第四步] 剧情对照表
   ├── 单剧本：3列（节点 | 节点名称 | 情节描述）
   └── 双剧本：4列（节点 | 我方节点名称 | 我方情节 | 对方情节）
               ★ 在对方情节列末尾标注对应关系，无对应节点直接留空
   │
   ▼
[第五步] 综合风险判断（仅双剧本）
   ├── 过滤层：剔除不受保护内容（题材/公式桥段/扁平原型/公有领域）
   ├── 人物相似密度 = 实质相似角色对数 / min(我方数, 对方数)
   ├── 情节相似密度 = 我方实质相似节点数 / 我方总节点数
   ├── 风险结论：低 / 中 / 高（阈值 50% / 70%）
   └── 修改建议：具体角色 + 具体节点编号的可操作方向
```

---

## 核心数据结构

### 人物节点
```
角色
├── 姓名（或功能标签）
├── 身份背景
├── 核心性格特征
├── 叙事功能
└── 关系网络（与其他角色的情感/利益纽带）
```

### 情节节点
```
节点
├── 编号（全局连续）
├── 节点名称
├── 核心事件
├── 因果关系（触发条件 + 结果）
└── ★ 对应标注（双剧本时写在"对方情节"列末尾，格式：★对应我方X）
    ├── 有对应 → ★对应我方X
    ├── 无对应 → 该行对方情节列留空
    └── 对方独有节点 → 表格末尾追加行，我方节点编号和名称列留空
```

### 风险量化
```
人物相似密度：实质相似角色对数 / min(我方角色数, 对方角色数)
情节相似密度：我方实质相似节点数 / 我方总节点数

风险等级：
  两项均 < 50%          → 低风险
  任一在 50%–70% 之间   → 中风险
  任一 > 70%            → 高风险
```

---

## 法律框架

基于"**抽象-过滤-比较**"三步法（中国著作权法司法实践）：

| 类别 | 内容 | 是否受保护 |
|---|---|---|
| 题材/类型标签 | 霸总、穿越、重生、宫斗 | 否 |
| 公式化桥段 | 误会→和好、失忆→相认 | 否 |
| 扁平人物原型 | 霸道总裁、傻白甜 | 否 |
| 公有领域素材 | 行业惯例叙事弧光 | 否 |
| 角色关系网络 | 多角色具体设定的特定组合方式 | **是** |
| 情节链条 | 多事件按一致顺序+因果串联 | **是** |
| 场景独特实现 | 道具/动作/对话情境的具体组合 | **是** |
| 特征性台词 | 具有人物个性的表达方式 | **是** |

关键阈值：**70%**——从被诉作品视角，独创性表达高度吻合超过70%，实质性相似基本成立。

---

## 设计决策说明

**为什么把格式转换和分段放在 prepare.py 而不是 SKILL.md**
格式转换（docx/pdf→txt）和字符计数是确定性操作，放在 prompt 里既浪费 token 又不可靠；脚本处理后只把纯文本传给 Claude，每次上下文更小、更干净。SKILL.md 保持纯 prompt，跨平台兼容性不受影响。

**为什么情节相似密度以"我方视角"计算**
法律诉讼中被诉方（我方）的举证责任在于证明自己作品的独创性，因此用"我方有多少内容与对方高度相似"来衡量风险更具实际意义。

**为什么不强制对齐剧情表的行数**
两个剧本的场景数量天然不同，强制对齐会扭曲情节结构；改用 ★ 标注跨行对应，更准确地反映真实关联。

**为什么空白单元格不填占位文字**
"*(待填入)*"之类占位符会干扰后续用 export.py 导出的表格视觉，也会让 AI 在双剧本对比时误判为有内容；约定为空即为空，由用户自行填写对方剧本数据。
