---
name: drama-copyright-check
description: 短剧剧本版权侵权分析工具。当用户提到剧本侵权、版权分析、剧本对比、侵权风险、著作权、短剧合规，或者粘贴剧本内容询问是否有版权风险时，必须使用此 skill。即使用户只说"帮我看看这个剧本有没有抄袭"或"对比一下这两个剧本"也应触发。核心输出是两张结构化对照表（人物表 + 剧情表），双剧本时追加综合风险判断。
---

# 短剧剧本版权侵权分析

## 你的任务

**⚠️ 强制规则：输出文件标题格式**

Markdown 文件的 H1 标题必须包含剧本名称，格式为：
```
# 《剧本名称》— 版权侵权分析
```
不得使用"剧本结构分析"或其他措辞替代"版权侵权分析"。

基于中国著作权法司法实践，对短剧剧本进行版权侵权分析，输出结构化对照表，帮助用户判断侵权风险或整理创作结构。

---

## 第零步：输入识别与确认

**判断输入类型：**

- 收到 **2个剧本** → 询问用户：「请问哪个是您的剧本（我方），哪个是对比的剧本（对方）？」若用户已通过标注（如"我的剧本""对方剧本"）明确，则直接进入下一步。

- 收到 **1个剧本** → 询问用户：「您是否需要生成带对比列的表格（对方列留空，方便后续填入）？」
  - 用户选是 → 生成含空白对方列的完整表格（对方列单元格直接留空，不填任何占位文字）
  - 用户选否 → 只生成我方单列表格

---

## 第一步：长度检测与分段决策

在提取内容前，先评估每个剧本的文本量：

**判断标准（估算）：**
- 单个剧本 **8万字以内** → 直接整体处理
- 单个剧本 **超过8万字** → 启动分段处理

**分段处理流程：**
1. 识别分集标记（"第X集"、"EP.X"、"集数+数字"等格式）自动切分
2. 无明确分集标记时，按约5万字均分
3. 对每个分段独立提取人物与情节
4. 提取完成后合并：
   - **人物**：跨段去重，同一角色出现在多段时合并描述
   - **情节节点**：按顺序拼接，保持全局连续编号

告知用户当前处于哪种模式（直接处理 / 分段处理），分段时说明分了几段。

---

**⚠️ 强制规则：分段文件由 skill 自动读取，用户无需手动传入内容**

当用户提供的剧本已由 prepare.py 预切分（输出目录下存在 part_1.txt、part_2.txt… 等文件）时：

- **不得询问用户"请传入下一段"**，分段是纯粹的技术机制，对用户完全透明。
- 使用文件读取工具，按编号顺序**自动读取全部 part 文件**（part_1.txt → part_2.txt → … → part_N.txt）。
- 每读一段，独立提取该段的人物与情节节点，作为中间结果暂存，**不向用户输出中间提取内容**。
- 全部分段读取完成后，执行合并（人物跨段去重、情节节点保持全局连续编号），然后直接进入第二步的输出阶段。

---

**⚠️ 强制规则：双剧本长度差异时，剧情比对只取对方对应比例段落**

当双剧本字数差异显著（对方明显长于我方）时：

- **人物比对**：始终使用对方**全文**提取的完整人物表（角色可能在任意章节出现）。
- **剧情比对**：找出对方相对应的部分情节段进行对比。

**示例**：我方 1.5 万字，对方 75 万字，R = 2%，对方取前 2% 的情节节点（约对应其开篇章节）参与比对。

---

## 第一步附：前置摘要输出

在输出元数据区块（字数 / 处理模式 / 输出模式）之后、人物对照表之前，先输出以下两节：

**选题**（Markdown 标题：`## 选题`）

列出 3–5 条适合该剧本的内容创作 / 宣发 / 平台推广选题，每条为一句话标题，突出设定亮点、情绪爽点或差异化卖点，不超过 40 字 / 条，无序列表格式。

**剧情梗概**（Markdown 标题：`## 剧情梗概`）

300–500 字连续段落，覆盖：主角起点与困境 → 觉醒 / 转折 → 主要冲突线 → 当前进展与核心悬念。语气简练，如同给编审看的一页纸概要。

---

## 第二步：提取结构

从每个剧本中提取以下内容：

**人物信息：**
- 所有有名有姓或有明确功能的角色
- 每个角色的：身份背景、核心性格特征、在剧中的叙事功能
- 角色之间的关系网络（谁爱谁、谁对抗谁、谁帮谁、情感/利益纽带）

**情节节点：**
- 场景级粒度：每个有独立叙事功能的场景算一个节点
- 每个节点包括：节点名称 + 核心事件 + 触发/结果（因果关系）
- 按故事时间顺序编号

---

## 第三步：生成人物对照表

输出格式如下。每个角色独立一行，最后单独一行描述关系网络。

**⚠️ 强制规则：表格单元格内禁止使用 `|` 字符**

表格内所有单元格的内容（包括人物关系网络行）中，不得用 `|` 作为分隔符，否则 Markdown 渲染会将其识别为新列，导致表格结构错乱。
- 关系网络中多条关系之间用 `；` 分隔
- 示例（正确）：`司律←兄妹→司音；司律←敌对→张浩；司律←契约伙伴→阿青`
- 示例（错误）：`司律←兄妹→司音 | 司律←敌对→张浩 | 司律←契约伙伴→阿青`

**单剧本（仅我方）：**

| 角色 | 人设描述（我方） |
|---|---|
| 男主 [姓名] | 身份+性格+叙事功能 |
| 女主 [姓名] | 身份+性格+叙事功能 |
| [其他角色逐一列出] | |
| **人物关系网络** | 各角色间的关系结构描述 |

**双剧本（我方 vs 对方）：**

| 角色 | 我方剧本人设 | 对方剧本人设 |
|---|---|---|
| 男主 | 身份+性格+叙事功能 | 身份+性格+叙事功能 |
| 女主 | ... | ... |
| [其他角色逐一对应] | | |
| **人物关系网络** | 我方关系结构 | 对方关系结构 |

若两个剧本角色数量不同，对方剧本中独有的角色也单独列出，我方那列标注"无对应角色"。

---

## 第四步：生成剧情对照表

两个剧本的节点各自独立展开，不强制对齐行数。相似节点用 ★ 标注对应关系。

**⚠️ 强制规则：禁止在表格内部插入分集标题行**

不得在剧情对照表内部穿插 `| **第X集：xxx** |` 形式的标题行。这类行列数与表格不符，会破坏 Markdown 渲染。如需体现分集结构，在节点名称列中直接写 `第X集·节点名称` 即可，无需另起标题行。

**单剧本（仅我方）：**

| 节点 | 节点名称 | 情节描述 |
|---|---|---|
| 1 | [名称] | 具体事件+因果关系 |
| 2 | ... | ... |

**双剧本（我方 vs 对方）：**

| 节点 | 我方节点名称 | 我方情节 | 对方情节 | 相似理由 |
|---|---|---|---|---|
| 1 | 开篇/人设建立 | 具体描述 | 具体描述 ★对应我方1 | 触发装置相同，均为XX引发XX结果 |
| 2 | ... | ... | 具体描述 ★对应我方2 | 主角行为路径重合：均通过XX手段达成XX目的 |
| 3 | ... | ... |  |  |

★ 标注规则：
- 在**对方情节**列末尾标注 ★对应我方X（如 ★对应我方3）
- 若对方某节点与我方多个节点相关，标注最接近的那个
- 若对方无对应节点，该行对方情节列与相似理由列均**留空**，不填任何占位文字
- 对方独有节点单独在表格末尾追加行，我方节点和名称列留空

**相似理由填写规则：**
- 仅在对方情节列有内容时填写，无对应节点时留空
- 说明构成相似的具体维度，可选：情节装置相同 / 因果逻辑一致 / 主角行为路径重合 / 场景功能对应 / 叙事顺序一致
- 一句话，不超过 30 字，聚焦最核心的相似点

---

## 第五步：综合风险判断（仅双剧本）

完成两张表后，基于表格内容输出以下判断。不需要重复表格内容，直接给结论。

### 【过滤层分析】

指出哪些表面相似点属于不受保护内容，应从相似度计算中剔除：
- **题材/主题**：如"霸总+萌宝"等类型标签
- **公式化桥段**：如"误会→和好""失忆→相认"等行业通用叙事
- **扁平人物原型**：如"霸道总裁""傻白甜女主"等未具体化的标签
- **公有领域素材**：穿越、重生、宫斗等通用设定

### 【人物相似密度】

```
实质相似角色对数 / min(我方角色数, 对方角色数) = X/Y = Z%
对比阈值：70%
```

说明哪些角色构成实质相似（人设+关系网络高度对应），哪些不构成。

### 【情节相似密度】

```
我方与对方实质相似的节点数 / 我方总节点数 = X/Y = Z%
对比阈值：70%（从我方视角：我方有多少内容来自对方）
```

注意：判断基准是**我方剧本视角**——"我方剧本里有多大比例的独创性表达与对方高度相似"，而非"用了对方多少"。

### 【风险结论】

**低风险 / 中风险 / 高风险** + 一句核心理由

参考：
- 两项密度均低于50% → 低风险
- 任一密度在50%-70%之间 → 中风险，需修改
- 任一密度超过70% → 高风险，构成实质性相似可能

### 【修改建议】

针对高相似度的具体角色和情节节点，给出可操作的修改方向：
- 指出具体角色名和需要改变的维度（人设/关系/功能）
- 指出具体节点编号和需要改变的内容（顺序/因果/细节实现方式）
- 参考策略：增加独有角色打破谱系对应、替换核心情节装置、改变人物关系权力结构

---

## 法律框架背景

本分析基于中国著作权法司法实践中的"抽象-过滤-比较"方法：

**不受保护的内容（过滤掉）：**
- 题材、类型标签（霸总、穿越、重生等）
- 有限表达：受题材制约不可避免出现的叙事弧光
- 扁平人物原型：仅有身份+单一性格标签、未形成独特弧光的角色
- 公有领域素材与行业惯例桥段

**受保护的内容（比对重点）：**
- 多个角色的具体设定按特定方式组合成的关系网络
- 多个事件按高度一致的顺序、因果逻辑串联成的情节链条
- 特定场景的独特实现方式（道具、动作、对话情境的具体组合）
- 具有人物特征性的台词表达方式

**关键阈值：** 从被诉作品视角，当70%以上内容与原作独创性表达高度吻合时，实质性相似基本成立。
